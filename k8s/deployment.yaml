apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-backend
  labels:
    app: python-backend

spec:
  replicas: 1
  selector:
    matchLabels:
      app: python-backend
  template:
    metadata:
      labels:
        app: python-backend
    spec:
      containers:
      - name: python-backend
        image: aartemdevops10/python-backend:baec5554a8d686327b29e1e2b533c8aeb9a14828
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        resources:
          requests:
            memory: "50Mi"
            cpu: "64m"
          limits:
            memory: "100Mi"
            cpu: "128m"
